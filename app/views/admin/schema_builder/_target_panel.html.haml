-# placeholder — sera enrichi en E4 avec la cascade
%div{ data: { controller: 'cascade-select', cascade_select_target_type_value: target.target_type, cascade_select_demarche_id_value: target.demarche_id, cascade_select_schema_target_id_value: target.id } }
  %p.text-muted Panel #{target.target_type} — workspace/app/table à venir (E4)
